services:
  qdrant:
    image: qdrant/qdrant:v1.11.0
    container_name: mesa-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"   # HTTP API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant-data:/qdrant/storage

volumes:
  qdrant-data:
